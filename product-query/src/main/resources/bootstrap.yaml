spring:
  application:
    name: product-query

kafka:
  bootstrap-servers: kafka:29092
  topic:
    product-event: product-event.t

cassandra:
  contact-point: cassandra
  keyspace: product_query
  table: product